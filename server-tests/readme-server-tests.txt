None of thest tests work until authentication token
can be injected somehow into the Impl file.

These tests run at the command line in the form
	perl register_service1
	perl register_service2
and so on.
